# 为 GeoQuest 贡献

感谢参与 GeoQuest。贡献可以是代码、原创教学设计、可访问性改进、数据加工方法或文档修正，但都必须遵守 [`docs/PROJECT_CONSTRAINTS.md`](docs/PROJECT_CONSTRAINTS.md)。

## 开始之前

1. 先搜索已有 issue，较大的主题或架构变更先提出讨论。
2. 从最新 `main` 创建短期分支，保持一次变更只解决一个清晰问题。
3. Node 工程使用 pnpm；Python 数据工具使用 UV。不要混入新的锁文件。
4. 不要把教材/课标源文件、扫描页、内部定位、私有审核证据、机器路径或待审核资产放入仓库、issue、PR 描述和截图。

## 代码与内容要求

- TypeScript 保持严格模式；优先复用组件，避免为简单效果增加大型依赖。
- 地图必须经 `MapProviderAdapter`；主题组件不得写死第三方瓦片地址。
- 核心教学流程不能依赖在线 API、公共 CDN、登录、Cookie 或第三方脚本。
- 每个主题必须有核心问题、唯一主操作、结构化证据和反思，并同时支持探索与课堂模式。
- 自由文本反思不得持久化。浏览器持久数据只使用 `geoquest:` 命名空间。
- 数据、地图、媒体和模拟必须按来源类型记录。原创模拟需说明方法与局限，不能伪装为实测数据。
- `approved` 只能由真实人工审核产生；脚本、测试、批量迁移和贡献者不得代替审核人赋值。

## 许可与署名

提交代码即表示你同意按 MIT License 提供该贡献；提交原创教学内容或文档即表示你有权按 CC BY-NC-SA 4.0 提供该贡献。第三方材料必须同时提供原始网址、权利人/机构、许可版本、获取日期、修改说明和准确署名。许可不清、仅限个人使用或禁止再发布的材料不会合并。

## 本地检查

```bash
pnpm install
pnpm check:boundary
pnpm lint
pnpm typecheck
pnpm test:run
pnpm build
```

涉及交互时，另行检查：

- `/topics/:slug` 与 `?mode=classroom` 使用同一事实、快照和答案逻辑。
- 键盘、触屏、减少动态、屏幕阅读器描述和数据表替代。
- 360×640、1366×768 以及断网/外部请求失败情形。
- 主题重置、本主题进度清除和全站本机数据清除。

## Pull Request

PR 应说明用户可见变化、验证方式、数据/素材来源、离线降级、可访问性影响以及仍待人工审核的门禁。不要用“测试通过”宣称内容、地图或版权已经获批。维护者可以要求拆分变更、补充来源或将状态降为 `unreviewed`/`blocked`。
